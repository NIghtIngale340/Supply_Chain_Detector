# Validation Checkpoints

## Phase 1

- [ ] Toolchain checks pass (`python`, `poetry`, `docker`)
- [ ] Dependencies installed successfully (`poetry install`)
- [ ] Lint baseline passes (`poetry run ruff check .`)
- [ ] Test baseline passes (`poetry run pytest -q`)
- [ ] Fetch PyPI metadata for 1 known package
- [ ] Fetch npm metadata for 1 known package
- [ ] Archive extraction works on at least one package source artifact
- [ ] Normalized record saved to `data/processed/`
